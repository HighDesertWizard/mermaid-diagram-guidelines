# Project Rules

## Frozen Mermaid Code Blocks

**Mermaid code blocks in EXAMPLES.md are frozen.** Do not modify, regenerate, or reformat any mermaid code block in EXAMPLES.md. If a task requires reordering sections, removing examples, or editing surrounding markdown, transfer the mermaid code blocks exactly as-is — byte-for-byte. Only modify a mermaid code block when explicitly asked to fix a specific diagram.
